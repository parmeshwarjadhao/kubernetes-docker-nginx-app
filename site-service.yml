apiVersion: v1
kind: Service
metadata:
  name: kubernetes-docker-nginx-static-html-demo
spec:
  selector:
    app: kubernetes   # ✅ MUST match Deployment label
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort
